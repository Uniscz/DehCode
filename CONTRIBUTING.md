# Contributing
Use Python 3.10 or newer and a virtual environment. Install with `pip install -e .`.
Run `python -m unittest discover -s tests -v` before submitting a pull request.
Keep adapters separate from orchestration. Cite official sources for model requirements;
mark estimates and unknowns. Never submit weights, credentials or claimed GPU results
without an actual run. Each adapter needs input validation and offline contract tests.
